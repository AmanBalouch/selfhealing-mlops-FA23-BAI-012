groups:
  - name: ml-drift-alerts
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.57
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence dropped below threshold"
          description: "prediction_confidence_score below 0.57 for more than 1 minute"